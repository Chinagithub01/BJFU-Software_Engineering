<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.UserMapper">

    <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO dbo.users (username, password_hash, real_name, role_id, is_active)
        VALUES (#{username}, #{passwordHash}, #{realName}, #{roleId}, 1)
    </insert>

    <select id="selectUserByUsername" resultType="User">
        SELECT * FROM dbo.users WHERE username = #{username} AND is_active = 1
    </select>

    <select id="selectUserById" resultType="User">
        SELECT * FROM dbo.users WHERE user_id = #{userId}
    </select>

    <update id="updateProfile" parameterType="User">
        UPDATE dbo.users SET nickname = #{nickname}, phone = #{phone}, email = #{email},
            school = #{school}, class_name = #{className}, student_id = #{studentId}
        WHERE user_id = #{userId}
    </update>

</mapper>