# 本地 IDE / 打包用占位配置（实际数据库连接由 Docker entrypoint 注入）
# Docker 部署使用 PostgreSQL，见 docker-compose.yml
jdbc.driver=org.postgresql.Driver
jdbc.url=jdbc:postgresql://localhost:5432/peerreview
jdbc.username=peerreview
jdbc.password=PeerReview@2026
